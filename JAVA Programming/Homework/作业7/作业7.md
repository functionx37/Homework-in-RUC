# TCP网络连接

文件的上传和下载：

1. 服务器端可以接受多个客户上传文件（可以给每个客户单列一个目录用于上传文件），也可接受多个客户从服务器端下载文件（可以单列一个目录，该目录下的文件所有客户均有权限下载，但没权限改动）。（必交部分）

2. 增加拷贝文件的进度条功能。（可选）

3. 进一步对于大文件下载，在客户端可以采用多线程模式，同时用多个线程下载文件增加文件下载速度。（可选）